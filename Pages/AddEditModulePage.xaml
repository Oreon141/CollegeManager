<Page x:Class="CollegeManager.Pages.AddEditModulePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.openxmlformats.org/expression/blend/2008" 
      mc:Ignorable="d" 
      Background="{StaticResource BackgroundBrush}"
      Loaded="Page_Loaded"
      Title="Добавление/Редактирование модуля">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0"
                Content="← Назад"
                Width="120"
                Height="40"
                FontSize="16"
                Background="#475569"
                Foreground="White"
                Margin="0 0 0 10"
                HorizontalAlignment="Left"
                Click="BackButton_Click"/>

        <TextBlock Grid.Row="1"
                   x:Name="HeaderText"
                   Text="Редактирование модуля"
                   FontSize="32"
                   FontWeight="Bold"
                   Foreground="#1e293b"
                   Margin="0 0 0 20"/>

        <Border Grid.Row="2"
                Style="{StaticResource Card}"
                Padding="25">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Порядковый номер" FontSize="18"/>
                    <TextBox x:Name="ModuleNumberBox" Height="40" FontSize="16" Margin="0 0 0 15" Padding="8"/>

                    <TextBlock Text="Название модуля" FontSize="18"/>
                    <TextBox x:Name="ModuleNameBox" Height="40" FontSize="16" Margin="0 0 0 15" Padding="8"/>

                    <TextBlock Text="Описание" FontSize="18"/>
                    <TextBox x:Name="ModuleDescriptionBox" Height="90" TextWrapping="Wrap" AcceptsReturn="True" FontSize="16" Margin="0 0 0 15" Padding="8"/>

                    <Grid Margin="0 0 0 15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="План. начало" FontSize="18"/>
                            <DatePicker x:Name="PlannedStartBox" Height="40"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="План. конец" FontSize="18"/>
                            <DatePicker x:Name="PlannedEndBox" Height="40"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0 0 0 15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Факт. начало" FontSize="18"/>
                            <DatePicker x:Name="ActualStartBox" Height="40"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Факт. конец" FontSize="18"/>
                            <DatePicker x:Name="ActualEndBox" Height="40"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Статус" FontSize="18"/>
                    <ComboBox x:Name="StatusBox" Height="40" FontSize="16" Margin="0 0 0 25" DisplayMemberPath="StatusName"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button x:Name="SaveButton" Content="Сохранить" Width="140" Height="45" FontSize="18" Background="#16a34a" Foreground="White" BorderThickness="0" Click="SaveButton_Click"/>
                        <Button x:Name="DeleteButton" Content="Удалить" Width="140" Height="45" Margin="15 0 0 0" FontSize="18" Background="#dc2626" Foreground="White" BorderThickness="0" Click="DeleteButton_Click" Visibility="Collapsed"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>