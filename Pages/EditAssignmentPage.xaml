<Page x:Class="CollegeManager.Pages.EditAssignmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Background="{StaticResource BackgroundBrush}"
      Title="Редактирование задания">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 25">
                <TextBlock Text="Редактирование задания"
                           FontSize="34"
                           FontWeight="Bold"
                           Foreground="#1e293b"
                           VerticalAlignment="Center"/>

                <Button Content="← Назад"
                        Height="48"
                        Padding="30 0"
                        Margin="25 0 0 0"
                        Background="#475569"
                        Foreground="White"
                        FontSize="20"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="Back_Click"/>
            </StackPanel>

            <Border Grid.Row="1"
                    Style="{StaticResource Card}"
                    Padding="35">
                <StackPanel Width="650">

                    <TextBlock Text="Название задания" FontSize="22" FontWeight="SemiBold" Foreground="#334155"/>
                    <TextBox x:Name="AssignmentNameBox" Height="58" Margin="0 10 0 25" FontSize="20" Padding="12" Background="#f1f5f9" BorderBrush="#cbd5e1"/>

                    <TextBlock Text="Описание" FontSize="22" FontWeight="SemiBold" Foreground="#334155"/>
                    <TextBox x:Name="DescriptionBox" Height="120" Margin="0 10 0 25" FontSize="20" Padding="12" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Background="#f1f5f9" BorderBrush="#cbd5e1"/>

                    <TextBlock Text="Статус" FontSize="22" FontWeight="SemiBold" Foreground="#334155"/>
                    <ComboBox x:Name="StatusBox" VerticalContentAlignment="Center" Cursor="Hand" Height="48" Margin="0 10 0 25" FontSize="20" Padding="8" Background="#f1f5f9" BorderBrush="#cbd5e1" DisplayMemberPath="StatusName"/>

                    <TextBlock Text="Исполнитель" FontSize="22" FontWeight="SemiBold" Foreground="#334155"/>
                    <ComboBox x:Name="UserBox" VerticalContentAlignment="Center" Cursor="Hand" Height="48" Margin="0 10 0 25" FontSize="20" Padding="8" Background="#f1f5f9" BorderBrush="#cbd5e1" DisplayMemberPath="Login"/>

                    <TextBlock Text="Вес (баллы/процент)" FontSize="22" FontWeight="SemiBold" Foreground="#334155"/>
                    <TextBox x:Name="WeightBox" Height="48" Margin="0 10 0 35" FontSize="20" Padding="8" Background="#f1f5f9" BorderBrush="#cbd5e1"/>

                    <Button Content="Сохранить изменения"
                            Height="52"
                            Width="290"
                            Margin="0 10 0 0"
                            Background="#2563eb"
                            Foreground="White"
                            FontSize="22"
                            FontWeight="SemiBold"
                            BorderThickness="0"
                            Cursor="Hand"
                            HorizontalAlignment="Left"
                            Click="Save_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>